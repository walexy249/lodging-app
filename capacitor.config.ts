import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.recipe.walexy.app',
  appName: 'recpe-app',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
